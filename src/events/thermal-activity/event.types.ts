/**
 * Thermal activity plugin — type-specific attributes.
 *
 * Facade over the canonical thermal attributes so the plugin stays self-contained.
 */
export type { ThermalEventAttributes } from '@/modules/environmental-events/types/environmental-event.types'
export type { ThermalObservationAttributes } from '@/modules/environmental-events/types/observation.types'
