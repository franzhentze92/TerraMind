/** Thermal activity plugin — priority factor provider (facade). */
export {
  thermalPriorityFactorProvider,
  THERMAL_PRIORITY_PROVIDER_ID,
} from '@/modules/environmental-events/thermal/thermal-priority-provider'
